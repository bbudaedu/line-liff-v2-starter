# This is the base URL for the backend Bot Server.
# For local development, if your bot server is running on port 3001,
# this would be http://localhost:3001
# When you deploy, this should be the public URL of your deployed bot server.
NEXT_PUBLIC_API_BASE_URL="http://localhost:3001"

# LIFF IDs for each page
# You can find these in the LIFF tab of your channel settings in the LINE Developers Console.
NEXT_PUBLIC_LIFF_ID_INDEX=""
NEXT_PUBLIC_LIFF_ID_CANCEL=""
NEXT_PUBLIC_LIFF_ID_TRANSPORT=""
